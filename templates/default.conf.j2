#
# {{ nginx_conf_dir }}/default.conf
#
server {
  listen 80 default_server;
  listen [::]:80 default_server;

  root {{ nginx_web_root }};

  index index.html;

  server_name _;

  location / {
    try_files $uri $uri/ =404;
  }
}
